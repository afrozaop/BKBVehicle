@model BKBVehicle.Models.VehicleAppModel.RepairViewModel
@{
    ViewBag.Title = "CreateOrganization";
}

<h2>CreateOrganization</h2>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <br />

        <h4>@TempData["retMsg"]</h4>
        <hr />

        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="row">

            <div class="col-md-4">
                @Html.Label("Add Organization Name")
                @Html.EditorFor(model => model.OrganizationName, new { htmlAttributes = new { @class = "form-control", @autocomplete = "off" } })
                @Html.ValidationMessageFor(model => model.OrganizationName, "", new { @class = "text-danger" })
            </div>
        </div>
        <br />
        <div class="row">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Add" class="btn btn-default btn-primary" />
                @Html.ActionLink("Cancel", "CreateOrganization", null, new { @class = "btn btn-default btn-primary" })
            </div>
        </div>
    </div>


}
